import React, { Component } from 'react'
import Timer from './Timer'

export default class Wpm extends Component {
    render() {
        return (
            <React.Fragment>
                <Timer/>
            </React.Fragment>
        )
    }
}
